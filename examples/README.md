# Examples Layout

This directory stores runnable examples and lightweight metadata.

- `cakeify/`: dataset download and preprocessing scripts.
- `data.json`: minimal metadata example for local debugging.

Static media files are moved to `assets/` to keep responsibilities clear.
