# Assets Layout

This directory stores static, reusable assets for examples and demos.

- `images/`: input images used by inference and training examples.
- `videos/`: optional lightweight sample videos (avoid large artifacts).

Do not place generated outputs here. Save generated results to `tmp/`.
